server:
  port: 8081

spring:
  application:
    name: exampleservice
  profiles:
    active:
      default

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/